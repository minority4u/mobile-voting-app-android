<?xml version="1.0" encoding="UTF-8" ?>
<ContentView
    x:Class="MSO.StimmApp.Views.Controls.AppStimmerEditorDetailsView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:attachments="clr-namespace:MSO.StimmApp.Views.Attachments;assembly=MSO.StimmApp"
    xmlns:controls="clr-namespace:MSO.StimmApp.Views.Controls;assembly=MSO.StimmApp"
    xmlns:forms="clr-namespace:FFImageLoading.Svg.Forms;assembly=FFImageLoading.Svg.Forms"
    xmlns:forms1="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
    xmlns:stimmApp="clr-namespace:MSO.StimmApp;assembly=MSO.StimmApp"
    xmlns:transformations="clr-namespace:FFImageLoading.Transformations;assembly=FFImageLoading.Transformations">
    <ContentView.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="150" />
            </Grid.RowDefinitions>

            <controls:AppStimmerEditorHeader Grid.Row="0" />
            <controls:AppStimmerAttachmentsListView Grid.Row="1" ItemsSource="{Binding AppStimmer.Attachments}" />

            <Grid Grid.Row="2" IsVisible="true">
                <forms:SvgCachedImage
                    HeightRequest="100"
                    HorizontalOptions="Center"
                    Source="resource://MSO.StimmApp.Resources.Icons.plus_circle.svg"
                    VerticalOptions="Center"
                    WidthRequest="100">

                    <forms1:CachedImage.Transformations>
                        <transformations:TintTransformation EnableSolidColor="true" HexColor="#ffffff" />
                    </forms1:CachedImage.Transformations>
                    <forms:SvgCachedImage.GestureRecognizers>
                        <TapGestureRecognizer Tapped="AddAttachmentImageButton_OnTapped" />
                    </forms:SvgCachedImage.GestureRecognizers>
                </forms:SvgCachedImage>
                <!--<Image
                    HorizontalOptions="Center"
                    Source="{stimmApp:ImageResource MSO.StimmApp.Resources.Icons.plus_orange_circle.scale-100.png}"
                    VerticalOptions="Center">
                    <Image.GestureRecognizers>
                        <TapGestureRecognizer Tapped="AddAttachmentImageButton_OnTapped" />
                    </Image.GestureRecognizers>
                </Image>-->
            </Grid>
            <Grid
                Grid.Row="2"
                BackgroundColor="White"
                IsVisible="{Binding IsAddingAttachment}">
                <attachments:AddAttachmentView />
            </Grid>
        </Grid>
    </ContentView.Content>
</ContentView>